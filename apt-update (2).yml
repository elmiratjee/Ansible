---
  - name: installing apt playbook
    hosts: all
    become: yes
    become_user: root    
    tasks:
      - name: Gathering all updates
        apt:
          upgrade: dist
      - name: Upgrade all packages on servers
        apt: 
          upgrade:  dist 
          force_apt_get:  yes